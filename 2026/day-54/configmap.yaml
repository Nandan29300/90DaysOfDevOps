apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: "production"
  APP_DEBUG: "false"
  APP_PORT: "8080"
  app.properties: |
    APP_NAME=MyApplication
    APP_VERSION=1.0.0
    LOG_LEVEL=info
    MAX_CONNECTIONS=100
