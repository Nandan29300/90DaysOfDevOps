# custom-values.yaml
# Custom values for nginx Helm chart

replicaCount: 3

service:
  type: NodePort
  nodePort: 30080

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"
